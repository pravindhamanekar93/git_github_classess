hello//


// git init -b main  used to create a new git repository with main branch 

// readme.md md means markdown language 

// Clone - To copy a repository from GitHub to your local machine
>> git clone <repository-url>

// ls -a means list all files including hidden files
>> ls -a
>> ls -Force for power shell


// status means show the status of the repository
>> git status

// modify > add > commit > push

// head means the latest commit

// git status has 4 typess

// 1. untracked - files that are not tracked by git
// 2. modified - files that are changed but not staged for commit
// 3. staged - files that are ready to be commited 
// 4. unmodified - files that are unchanged 


// staged means files that are ready to be commited 

// add - adds new or changed files in your working directory to the Git stating area
>> git add <filename>

// to add all the files use 
>> git add .

// commit - saves changes to the local repository with a descriptive message
>> git commit -m "commit message"

// push - uploads your local commits to a remote repository for sharing 
>> git push origin main

// git log - shows the commit history of the repository
>> git log

// git diff - shows the changes between the working directory and the staging area
>> git diff

// delete a filename
>> git rm --cached <filename>


----------------------------------------------

// create a new repo in command line


// echo "# hello ">> README.md
// git init
// git add README.md
// git commit -m "first commit"
// git branch -M main
// git remote add origin git@github.
// git push -u origin main

-----------------------------------



// git tag means versioning of the repository
// git tag -a v1.0 -m "version 1.0
// git tag
// git show v1.0
// push


-----------------------------------

// branch - used to create a new branch
// there are 3 types to create branches 

// 1. git checkout -b <branch-name>
// 2. git branch <branch-name>
// 3. git switch -c <branch-name> # latest versio 

// git branch is used to know how many branches are there in the repository
// git branch -all also be used to know all the barnches including remote branches

// to switch between branches use
// git switch <branch-name> or
// git checkout <branch-name>

// <git switch -> =means switches to previous branch

// to delete a branch
// git branch -d <branch-name>

// to push a branch
// git push origin <branch-name>

// to rename a branches
// git branch -M <new-branch-name>

--------------------------

// how to merge branches
// first switch to the main branch
// then type this command
// git merge <branch-name>


// last push in repository
// git push origin main



---------------------------------------------------

// git remote -v means to verify the repo

// git commit -a -m "commited" - commit all changes


---------------------------

pull command - used to download changes from the remote repository to local repository
>> git pull origin main


-- merge conflicts comming


------------------------------

git branchinga and merging

// create a new branch
// add contents
// push to new created branch

// diff between two branches
>> git diff main

// merge two barnches
>> git merge branch_name

or create a pull request through github website
next
write this command >> git pull origin main 
to fecth and download contents from a remote repo to local\repo



/////////// merge conflicts
an event that takes place when git is unable to automatically resolved differences in code between two commits


